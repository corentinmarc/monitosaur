export const fromMsToMinutes = (ms: number): number => ms / 60 / 1000;
export const fromMinutesToMs = (minutes: number): number => minutes * 60 * 1000;

export const fromBToGB = (bytes: number): number => bytes / 1024 / 1024 / 1024;
