.container {
  display: flex;
  flex-direction: column;
  height: 100%;
}

.metrics {
  display: flex;
  flex-direction: column;
  height: 100%;
  flex: 1;
  height: calc(100% - 140px);
  overflow: auto;
}
